apiVersion: v1
kind: Secret
metadata:
  name: agri-triage-secrets
type: Opaque
data:
  # To generate this on the server, we will use a manual kubectl command.
  # Leave this as a placeholder here for Git.
  GROQ_API_KEY: REPLACE_ME_ON_SERVER
